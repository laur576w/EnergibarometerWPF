<Window x:Class="LogInScreen.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogInScreen"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowState="Maximized" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="400" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="Error"  Grid.Column="1" MinHeight="100"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Width="150" Margin="10">
            <!-- Username -->
            <TextBlock Text="Brugernavn(Unilogin)" />
            <TextBox x:Name="Username"/>
               <!-- Password -->
            <StackPanel x:Name="StackPass">
                
            <TextBlock Text="Adgangskode" Margin="0 10 0 0"/>
            <PasswordBox x:Name="Password"/>
            </StackPanel>
        </StackPanel>

        <StackPanel x:Name="Names" Grid.Row="1" Grid.Column="1" Width="150" Margin="10 55 10 0" Visibility="Hidden">
            <TextBlock Text="Fornavn" />
            <TextBox x:Name="Firstname"/>

            <TextBlock Text="Efternavn"  Margin="0 10 0 0"/>
            <TextBox x:Name="Lastname"/>
        </StackPanel>
             
        <!-- Log in -->
        <Button  Content="Log in" Width="70" Height="25" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Margin="0 110 100 0"/>
        
        <!-- Create new account -->
        <Button Content="Lav konto" Width="70" Height="25" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Margin="0 110 -100 0" Click="Button_Click"/>
    </Grid>
</Window>
